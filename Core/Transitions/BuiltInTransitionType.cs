namespace Velentr.States.Transitions
{
    /// <summary>
    /// Values that represent built-in transition types.
    /// </summary>
    public enum BuiltInTransitionType
    {
        /// <summary>
        /// An enum constant representing the custom option (not using a built-in transition).
        /// </summary>
        Custom,

        /// <summary>
        /// An enum constant representing the Fade option.
        /// </summary>
        Fade,
    }
}